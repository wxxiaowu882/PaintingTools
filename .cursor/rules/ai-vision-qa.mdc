---
description: 标注/场景验收必须用 AI 视觉看图，禁止只靠探针或命中日志宣称通过
alwaysApply: true
---

# 验收必须用 AI 视觉（不是探针）

## 硬性要求

涉及 **3D 场景标注、引出线、虚线轮廓、点位是否贴形、明暗交界是否指对** 时：

1. **必须**对最终（或候选）**截图**做 **AI 视觉读图自检**（用 Read 打开 PNG，根据画面描述核对标签语义与几何位置）。
2. **禁止**仅凭 UV 探针命中、射线 `ok`、命中个数、JSON 已写入、脚本 exit 0 就宣称「标注正确 / 已验收」。
3. 探针与射线只用于**辅助定点**；**对错以画面为准**。

## 自检时至少核对

- 标签文字是否与所指部位一致（名称不能乱贴）。
- 点是否落在课上要讲的那条线上（外轮廓关 → 轮廓边缘；光位关 → 交界/特征影）。
- 虚线是否沿着目标轮廓/交界走，而不是贴在无关表面。
- 视角关与光位关是否串课（外轮廓关不要主讲光影证据，反之亦然）。

## 失败处理

视觉自检发现错位 / 错名 / 讲错侧（如侧前应标远端却标了近侧）→ **先改点位或文案，再截图，再视觉复检**；不得把「探针扫到了」当作通过。
