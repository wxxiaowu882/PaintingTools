---
description: 每次会话默认走模式1（planner→实现→verifier），用户无需另写提示语
alwaysApply: true
---

# 模式 1 默认流水线（自动执行）

本仓库 Agent 会话**默认**按 `.cursor/agents/` 架构运转。用户**不必**写 `/planner`、`/verifier` 或「先规划再验收」类提示语；除非用户明确说「跳过规划」「不用验收」「停」。

## 主 Agent 必须自动做的事

1. **开工**  
   - 多文件、多步骤、验收标准不清 → **主动派 planner**（Task / 子代理）。  
   - 单点极清楚的小改 → 可跳过 planner，但须在回复里用一句话说明为何跳过。

2. **实现**  
   - 由主 Agent 按计划（或跳过规划后的目标）最小 diff 落地。  
   - 每完成一个单点：向用户汇报进度（做了什么 / 结果 / 下一步）。

3. **验收**  
   - 某步或整单声称可交付前 → **主动派 verifier**。  
   - verifier 不通过 → 按反馈修改 → **再次派 verifier**；**无轮数上限**，直到通过或用户说停。  
   - 涉及页面/3D/标注：verifier 须主动开浏览器 + AI 视觉；主 Agent 不得用探针日志代替验收结论。

4. **清理**  
   - 与 verifier 一起：及时删除无用截图与过程文件（不影响后续即可删）。

## 用户一句话即可

用户只需说业务目标（例如「修某某」「做某某场景」）。主 Agent 自行规划 → 实现 → 验收循环，并中途汇报。

## 例外（才可不走全流程）

- 用户明确：「只要回答 / 不用改代码 / 跳过规划 / 跳过验收 / 停」  
- 纯问答、只读解释、不改仓库且无可验证交付物  

细则与角色说明见 `.cursor/agents/README.md`。
